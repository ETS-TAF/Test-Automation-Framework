apiVersion: v1
kind: ConfigMap
metadata:
  name: microservices-config-map
  namespace: taf
data:
  # property-like keys; each key maps to a simple value
  testapi_url: <TO UPDATE>
  testapi_port: <TO_UPDATE>
  gatling_url: <TO_UPDATE>
  gatling_port: <TO_UPDATE>
  jmeter_url: <TO_UPDATE>
  jmeter_port: <TO_UPDATE>
immutable: false